1. 修复CaptchaService.verify()方法中的逻辑错误：将第82-84行的条件判断从"if (optional.isPresent())"改为"if (!optional.isPresent())"，这样当验证码存在且未使用时才能继续验证
2. 修改UserController.uploadAvatar()方法中的图片大小限制：将硬编码的2MB限制改为与配置文件一致的10MB

